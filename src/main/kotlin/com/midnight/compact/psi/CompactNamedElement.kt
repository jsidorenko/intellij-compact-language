package com.midnight.compact.psi

import com.intellij.psi.PsiNameIdentifierOwner

/** A named, navigable declaration (circuit / witness / ledger / struct / enum / module name). */
interface CompactNamedElement : PsiNameIdentifierOwner
