package com.midnight.compact.psi.impl

// Naming (getName / getNameIdentifier / setName) is implemented directly in
// CompactNamedElementImpl, so no PsiImplUtil delegation is needed. Intentionally empty.
